{
  "name": "example_site-1",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.6.4"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "sass": "^1.43.4"
  }
}
